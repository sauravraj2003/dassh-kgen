FROM python:3.12-slim

WORKDIR /app

# Install dependencies
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copy source
COPY . .

EXPOSE 7700

CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "7700", "--workers", "2"]
