/**
 * llmService.ts — DEPRECATED
 * ----------------------------
 * Direct browser-to-AI API calls have been removed for security reasons.
 * All AI requests now go through the FastAPI backend (backend/main.py).
 *
 * Use apiService.ts instead:
 *   import { sendChatMessage, generateGame } from '@/lib/apiService';
 */

export {};