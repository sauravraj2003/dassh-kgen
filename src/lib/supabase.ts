/**
 * supabase.ts — REMOVED
 * ----------------------
 * Supabase has been replaced with custom JWT auth via FastAPI.
 * This file is kept as an empty stub to avoid breaking any stale imports.
 * All auth logic lives in src/lib/authService.ts
 */

export const supabase = null;
export const auth = null;